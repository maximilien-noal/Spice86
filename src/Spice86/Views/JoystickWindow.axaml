<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
        x:Class="Spice86.Views.JoystickWindow"
        xmlns:vm="using:Spice86.ViewModels"
        Icon="/Icon/Spice86.ico"
        CanResize="True"
        WindowStartupLocation="CenterOwner"
        Title="OnScreen Digital Joystick" MinWidth="600" MinHeight="300" Width="600" Height="300">
        <Design.DataContext>
                <vm:JoystickViewModel />
        </Design.DataContext>
        <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto" ColumnDefinitions="*,*">
                <TextBlock Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" Text="{Binding LastGamePortReadValue, StringFormat='Last game port value returned: 0x{0:X2}'}" />
                <TextBlock Grid.Column="0" HorizontalAlignment="Center" Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding LastGamePortReadTime, StringFormat='Last game port read time: {0}'}" />
                <TextBlock Grid.Column="0" HorizontalAlignment="Center" Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding LastGamePortWriteTime, StringFormat='Last game port write time: {0}'}" />
                <Button Grid.Column="0" Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Center" Content="Force Update" Command="{Binding UpdateDataCommand}" />
                <TextBlock Grid.Column="0" HorizontalAlignment="Center" Grid.Row="4" Grid.ColumnSpan="2" Text="{Binding LastUpdate, StringFormat='Last update: {0}'}" />
                <Separator Grid.Row="5" Grid.ColumnSpan="2" />
                <Grid Grid.Row="6" Grid.Column="0" ColumnDefinitions="*, *, *" RowDefinitions="*,*,*">
                        <Button Grid.Row="0" Grid.Column="1" Command="{Binding UpCommand}" Margin="1" MinWidth="40" Content="Up" />
                        <Button Grid.Row="2" Grid.Column="1" Command="{Binding DownCommand}" Margin="1" MinWidth="40" Content="Down" />
                        <Button Grid.Row="1" Grid.Column="0" Command="{Binding LeftCommand}" Margin="1" MinWidth="40" Content="Left" />
                        <Button Grid.Row="1" Grid.Column="2" Command="{Binding RightCommand}" Margin="1" MinWidth="40" Content="Right" />
                </Grid>
                <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Vertical">
                        <Button Margin="1" MinWidth="40" Content="Button A" Command="{Binding Button1Command}" />
                        <Button Margin="1" MinWidth="40" Content="Button B" Command="{Binding Button2Command}"/>
                </StackPanel>
        </Grid>
</Window>
